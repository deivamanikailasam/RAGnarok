# Copy to .env and edit. Never commit .env (see .gitignore).

# --- model endpoints (roles -> OpenAI-compatible base URLs) --- (Steps 2-3)
LLM_LARGE_BASE_URL=http://localhost:8001/v1
LLM_SMALL_BASE_URL=http://localhost:8002/v1
EMBEDDING_BASE_URL=http://localhost:7997
RERANKER_URL=http://localhost:7997

# --- infra --- (Step 1)
QDRANT_URL=http://localhost:6333
DATABASE_URL=postgresql://postgres:ragnarok@localhost:5432/ragnarok
REDIS_URL=redis://localhost:6379/0
PG_PASSWORD=ragnarok

# --- observability --- (Step 26)
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_SECRET=change-me-in-prod
LANGFUSE_SALT=change-me-in-prod

# --- slack (Step 23) ---
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=

# --- optional scale-out: leave blank to stay fully local (Step 3) ---
OPENAI_API_KEY=

# --- runtime ---
RAGNAROK_ENV=local
RAGNAROK_SETTINGS=config/settings.yaml
